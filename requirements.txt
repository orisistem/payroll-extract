# --- Framework principal ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- ORM / Banco de Dados ---
prisma
databases==0.9.0          # opcional, útil se quiser consultas assíncronas genéricas
aiosqlite==0.20.0         # driver SQLite async (necessário para Prisma com SQLite)

# --- Validação e Modelos ---
pydantic==2.9.2
pydantic-settings==2.4.0  # para config.py (env vars)

# --- Utilitários ---
python-dotenv==1.0.1      # carregar variáveis de ambiente
loguru==0.7.2             # logging estruturado e elegante

# --- PDF / Parsing / Exportação ---
PyMuPDF==1.24.10          # extração de texto de PDF
pandas==2.2.3             # exportação CSV, manipulação de dados

# --- Testes ---
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2             # testes de endpoints FastAPI
coverage==7.6.2           # cobertura de testes

# --- Desenvolvimento (opcional) ---
black==24.8.0             # formatação automática
isort==5.13.2             # organização de imports
mypy==1.11.2              # checagem de tipos
ruff==0.6.4               # linting rápido
